[workspace]
members = [
  "utils",
  "math",
  "crypto",
  "fri",
  "air",
  "prover",
  "verifier",
  "examples"
]

[profile.release]
codegen-units = 1
lto = true

[profile.bench]
codegen-units = 1
lto = true
